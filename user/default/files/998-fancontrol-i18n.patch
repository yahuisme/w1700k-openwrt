--- a/htdocs/luci-static/resources/view/fan/settings.js
+++ b/htdocs/luci-static/resources/view/fan/settings.js
@@ -55,12 +55,12 @@
 	ctx.fillStyle = '#333';
 	ctx.font = '12px sans-serif';
 	ctx.textAlign = 'center';
-	ctx.fillText('Temperature (\u00B0C)', width / 2, height - 5);
+	ctx.fillText(_('Temperature (\u00B0C)'), width / 2, height - 5);
 
 	ctx.save();
 	ctx.translate(12, height / 2);
 	ctx.rotate(-Math.PI / 2);
-	ctx.fillText('PWM (0-255)', 0, 0);
+	ctx.fillText(_('PWM (0-255)'), 0, 0);
 	ctx.restore();
 
 	ctx.textAlign = 'center';
@@ -121,7 +121,7 @@
 		ctx.fillRect(width - 100, legendY, 15, 15);
 		ctx.fillStyle = '#333';
 		ctx.textAlign = 'left';
-		ctx.fillText(preset.charAt(0).toUpperCase() + preset.slice(1), width - 80, legendY + 12);
+		ctx.fillText(_(preset.charAt(0).toUpperCase() + preset.slice(1)), width - 80, legendY + 12);
 		legendY += 20;
 	});
 }
--- a/htdocs/luci-static/resources/view/fan/status.js
+++ b/htdocs/luci-static/resources/view/fan/status.js
@@ -88,7 +88,7 @@
 		status = status || {};
 
 		var modeClass = status.fan_mode === 2 ? 'label-success' : 'label-warning';
-		var modeText = status.fan_mode_desc || 'Unknown';
+		var modeText = _(status.fan_mode_desc || 'Unknown');
 
 		var viewEl = E('div', { 'class': 'cbi-map' }, [
 			E('h2', {}, _('Fan Control - Status')),
@@ -109,8 +109,8 @@
 						E('div', { 'class': 'cbi-value-field' }, [
 							E('span', { 'id': 'fan-preset' },
 								status.uci_mode === 'manual' ? _('Manual Override') :
-								(status.uci_preset || 'balanced').charAt(0).toUpperCase() +
-								(status.uci_preset || 'balanced').slice(1))
+								_((status.uci_preset || 'balanced').charAt(0).toUpperCase() +
+								(status.uci_preset || 'balanced').slice(1)))
 						])
 					])
 				])
@@ -164,7 +164,7 @@
 				// Update mode display
 				var modeEl = document.getElementById('fan-mode');
 				if (modeEl) {
-					modeEl.textContent = status.fan_mode_desc || 'Unknown';
+					modeEl.textContent = _(status.fan_mode_desc || 'Unknown');
 					modeEl.className = status.fan_mode === 2 ? 'label-success' : 'label-warning';
 				}
 
@@ -172,8 +172,8 @@
 				var presetEl = document.getElementById('fan-preset');
 				if (presetEl) {
 					presetEl.textContent = status.uci_mode === 'manual' ? _('Manual Override') :
-						(status.uci_preset || 'balanced').charAt(0).toUpperCase() +
-						(status.uci_preset || 'balanced').slice(1);
+						_((status.uci_preset || 'balanced').charAt(0).toUpperCase() +
+						(status.uci_preset || 'balanced').slice(1));
 				}
 			}, this));
 		}, this), 3);
