--- a/package/network/config/bridge-hw-offload/files/etc/init.d/bridge-hw-offload
+++ b/package/network/config/bridge-hw-offload/files/etc/init.d/bridge-hw-offload
@@ -11,9 +11,9 @@
 START=21
 
 start_service() {
-	procd_openinstance
+	procd_open_instance
 	procd_set_param command /bin/true
-	procd_closeinstance
+	procd_close_instance
 }
 
 service_triggers() {
--- a/package/network/config/bridge-hw-offload/files/usr/share/bridge-hw-offload/apply-rules.sh
+++ b/package/network/config/bridge-hw-offload/files/usr/share/bridge-hw-offload/apply-rules.sh
@@ -22,19 +22,15 @@
 }
 
 main() {
+    local devices
+    devices=$(detect_bridge_ports)
+
     # Only use the NPU/hardware path when fw4 hardware offload is enabled.
-    if [ "$(uci -q get firewall.@defaults[0].flow_offloading_hw)" != "1" ]; then
+    if [ "$(uci -q get firewall.@defaults[0].flow_offloading_hw)" != "1" ] || [ -z "$devices" ]; then
         rm -f "$RULES_FILE"
         nft delete table bridge fw4 >/dev/null 2>&1
-        logger -t bridge-hw-offload "flow_offloading_hw not set, hardware offload disabled"
+        logger -t bridge-hw-offload "Hardware offload disabled or no bridge ports for ${BRIDGE}"
     else
-        local devices
-        devices=$(detect_bridge_ports)
-        if [ -z "$devices" ]; then
-            logger -t bridge-hw-offload "No bridge ports found for ${BRIDGE}, skipping"
-            return 1
-        fi
-
         mkdir -p "$RULES_DIR"
         cat > "$RULES_FILE" <<EOF
 destroy table bridge fw4
